debug: false
spring:
  application:
    name: patient-service
  datasource:
    url: jdbc:h2:mem:patients
    username: root
    password: root
    driver-class-name: org.h2.Driver # SOme chaneg
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop # Set to create-drop to recreate the database every time the application starts
    show-sql: false # Set to true to see the SQL queries generated by Hibernate
    properties:
      hibernate:
        format_sql: true # Set to true to format the SQL queries
  h2:
    console:
      enabled: true
server:
  port: 8100
logging:
  level:
    root: WARN
    com:
      healthcare:
        patient_service:
          controller: DEBUG
          service: DEBUG
          repository: DEBUG
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
  pattern:
    console: "%msg%n"